/* 2 - Criar um comando SELECT que retorne o nome do cliente e o nome da cidade de todos os registros. */ 

SELECT nome_cliente, nome_cidade
FROM clientes
INNER JOIN cidades
	ON codigo_cidade = cidades_codigo_cidade;